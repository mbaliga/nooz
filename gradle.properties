org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true
android.defaults.buildfeatures.buildconfig=false

kotlin.code.style=official

# Package base is centralized here so the late RESERVED rename sweep touches
# exactly one line + each module namespace derives from it. See STATE.md.
# Renamed 2026-07 to the Play Console-registered package.
riverwip.packageBase=dev.asystemofcells.nooz
